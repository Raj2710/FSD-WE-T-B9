## Steps to Deploy in Netlify or Vercel

1. Select the repo and branch correctly
2. Source Directory should be selected as src.
3. Build command should be given as npm run build
4. Inorder to ensure step 3 works make sure you added the build command in package.json
5. Always add node_modules to the .gitIgnore files before pusing the code. 